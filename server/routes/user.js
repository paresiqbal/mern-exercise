// Library
import express from "express";

// router setup
const router = express.Router();

// login route
router.post("/login", () => {});

// signup route
router.post("/signup", () => {});

export { router as userRouter };
